!!! 5
%head
  %title Daitss
  = google_font_link_tag :Arimo, :regular, :italic, :bold, :bolditalic
  = google_font_link_tag :Cabin, :regular, :italic, :bold, :bolditalic

  = stylesheet_link_tag :all
  = javascript_include_tag :defaults
  = csrf_meta_tag

%body

  %nav
    %span.breadcrumbs= breadcrumbs
    %span.version= Daitss::VERSION

  - if flash[:notice]
    .notice= flash.notice
  - if flash[:alert]
    .notice.alert= flash.alert

#content= yield
