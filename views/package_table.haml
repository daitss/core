%table
  %tr
    %th package
    %th account
    %th activity
    %th time
  - packages.each do |p|
    %tr
      %td
        %a{:href => "/package/#{p.id}"}= p.id
      %td= p.project.account.id rescue "N/A"
      - latest_event = p.events.last
      %td= latest_event.name
      %td= latest_event.timestamp.pragma
